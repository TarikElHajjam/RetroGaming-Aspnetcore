@model CreateUserViewModel
@{
    ViewBag.Title = "Create user";
}
<br />
<br />
<br />
<br />
<div class="login-form ">
    <h1>Create a new user</h1>
    <form method="post">
        <div asp-validation-summary="All" class="text-white"></div>
        <div class="form-group">
            <input id="input" asp-for="FullName" name="FullName" placeholder="Full name">
            <span class="input-icon" id="input-icon"><i class="ion ion-person"></i></span>
        </div>
        <div class="form-group">
            <input id="input" asp-for="UserName" placeholder="Username" />
            <span class="input-icon" id="input-icon"><i class="ion ion-person"></i></span>
            <div asp-validation-for="UserName" class="text-white"></div>
        </div>
        <div class="form-group">
            <input id="input" asp-for="Email" placeholder="Email Addresse" />
            <span class="input-icon" id="input-icon"><i class="ion-at"></i></span>
            <div asp-validation-for="Email" class="text-white"></div>
        </div>
        <div class="card mb-3" id="cardRoles">
            <div class="card-body">
                <div class="checkbox-Role">
                    @{
                        <p class="text-dark">Please, assign roles to this user</p>
                        foreach (var r in Model.AllRoles.ToList())
                        {

                            <label class="checkbox-LabelRole">
                                @r.Name
                                <input type="checkbox" class="checkbox-Input" asp-for="IsSelected" name="roleId" value="@r.Name" />
                                <span class="checkbox-Checked ">&check;</span>
                            </label>
                        }
                    }
                </div>
            </div>
        </div>
        <div class="form-group">
            <input id="input" asp-for="Password" placeholder="Password" />
            <span class="input-icon" id="input-icon"><i class="ion-locked"></i></span>
            <div asp-validation-for="Password" class="text-white"></div>
        </div>
        <div class="form-group">
            <input id="input" asp-for="ConfirmPassword" placeholder="Confirm Password" />
            <span class="input-icon" id="input-icon"><i class="ion-locked"></i></span>
            <div asp-validation-for="ConfirmPassword" class="text-white"></div>
        </div>
        <button class="login-btn" type="submit">Create</button>
    </form>
</div>
